kafka:
  enabled: true
redis:
  enabled: true
vault:
  enabled: true

apiGateway:
  replicaCount: 2
  image: svaani/api-gateway:latest
  port: 8080

fhirAdapter:
  replicaCount: 2
  image: svaani/fhir-adapter:latest
  port: 8080

clinicalNlp:
  replicaCount: 2
  image: svaani/clinical-nlp:latest
  port: 8080

sttEngine:
  replicaCount: 2
  image: svaani/stt-engine:latest
  port: 8080

audioProcessor:
  replicaCount: 2
  image: svaani/audio-processor:latest
  port: 8080
